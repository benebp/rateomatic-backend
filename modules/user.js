module.exports = class User {
	constructor({ id, name, role }) {
		this.id = id;
		this.name = name;
		this.role = role;
	}

	// milyen tulajdonsagai vannak egy user-nek
	// milyen methodok, miket tud csinalni

	// db-bol lekerni az adatait, filmjeit, baratait
};
